{
  "name": "server-casamento",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "engines": {
    "node": "18"
  },
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --watch --no-warnings server.js",
    "dev": "node --watch --no-warnings server.js",
    "build": "tsup server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^8.4.0",
    "fastify": "^3.16.1",
    "fastify-cors": "^6.1.0",
    "postgres": "^3.4.0",
    "stripe": "^14.1.0"
  },
  "devDependencies": {
    "dotenv": "^16.3.1",
    "tsup": "^7.2.0"
  }
}
